type Mutation {
	createTask(title: String!): Post
	deleteTask(id: ID!): Post
	updateTask(id: ID!, title: String): Post
}

type Task {
	id: ID!
	title: String!
	done: Bool!
}

type Query {
    Task(id: ID!): Task
    Tasks(limit: Int): [Task]	
}

schema {
	query: Query
	mutation: Mutation
}
