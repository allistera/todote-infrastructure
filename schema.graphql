type Mutation {
	putPost(title: String!): Post
}

type Post {
	id: ID!
	title: String!
}

type Query {
    	Post(id: ID!): Post
    	Posts(limit: Int, skip: Int, sort_field: String, sort_order: String): [Post]
	
}

schema {
	query: Query
	mutation: Mutation
}
